apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pc-price-tracker

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # Optional extras — uncomment when ready:
  # - ingress.yaml
  # - cronjob-refresh.yaml

images:
  - name: pc-price-tracker
    newName: pc-price-tracker
    newTag: local
